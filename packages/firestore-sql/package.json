{
  "name": "@firebase-desk/firestore-sql",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint --config ../config-oxlint/oxlintrc.base.json src",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "tsc -b tsconfig.build.json --force"
  },
  "dependencies": {
    "chevrotain": "12.0.0"
  },
  "devDependencies": {
    "@firebase-desk/config-oxlint": "workspace:*",
    "@firebase-desk/config-tsconfig": "workspace:*",
    "@firebase-desk/config-vitest": "workspace:*",
    "@types/node": "24.13.3",
    "oxlint": "1.75.0",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
